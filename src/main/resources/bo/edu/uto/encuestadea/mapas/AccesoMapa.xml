<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="bo.edu.uto.encuestadea.mapas.AccesoMapa">

	<select id="getDatosUsuario" resultType="UsuarioAcceso">
		 SELECT u.id_persona,u.id_usuario,u.apodo,u.clave,p.nombre_completo,
			'-1'::text as id_facultad, '-1'::integer as id_departamento
		  FROM personas p JOIN dea.usuario u USING(id_persona)
		  WHERE u.id_estado
			AND p.id_estado
			AND u.apodo = #{apodo};
	</select>
	
</mapper>
